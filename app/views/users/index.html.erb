<% provide(:title, "隊員") %>

<div class="pagination">
 <h1>隊員</h1>
 <%= will_paginate %>
 <ul class="users">
  <% @users.each do |user| %>
   <%= render user %>
  <% end %>
 </ul>
  <%= will_paginate %>
</div>
