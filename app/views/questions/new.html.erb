<aside class="col-md-12 offset-md-3">
 <%= form_for(@question) do |q| %>
   <div class="field">
   <%= q.text_field :title, class: "form-control", placeholder: "タイトル" %>
   <%= q.text_area :content, rows: 20, placeholder: "質問をする" %>
   <%= q.submit "投稿", class: "btn btn-primary" %>
   <span class="image">
   <%= q.file_field :image, accept:'image/jpeg,image/gif,image/png' %>
   </span>
   </div>
 <% end %>

  <script type="text/javascript">
    $('#post_image').bind('change', function(){
      var size_in_megabytes = this.files[0].size/1024/1024;
      if (size_in_megabytes >5){
        alert("最大ファイルは５MBまでです！小さいファイルを選び直してください！")
      }
    });
  </script>
  </aside>
</div>
